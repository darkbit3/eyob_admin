This folder stores product image assets used by the admin UI.
Add image files here (jpg/png/webp). Frontend displays one thumbnail in the product table and a gallery in the product detail modal.

Guidelines:
- Upload at least 3 images per product, up to 8 images.
- Filenames should be unique per product, e.g. `p-<productId>-1.jpg`.
